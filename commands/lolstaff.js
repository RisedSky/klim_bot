module.exports = {
    help: { name: "hfstaff" },
    run: async (call) => {
        //Staff League of Legends
        //message, bot, bot.commands, args, content, prefix, cmd
        var Mess_Member = call.message.member;


    }
}